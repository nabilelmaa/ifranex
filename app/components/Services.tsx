import { ServicesList } from "@/app/components/ServicesList";

export const Services = () => {
  return (
    <section id="services">
      <div className="px-4">
        <ServicesList />
      </div>
    </section>
  );
};
