import { SignInForm } from "@/app/components/forms/SignInForm";

const Page = () => {
  return (
    <div className="lg:flex lg:items-center lg:justify-center md:items-center md:justify-center min-h-screen bg-sky-50">
      <SignInForm />
    </div>
  );
};

export default Page;
